{
  "name": "cryptostock-frontend",
  "version": "1.0.0",
  "description": "Frontend for Gumball Crypto News",
  "scripts": {
    "start": "npx serve -s . -l $PORT",
    "dev": "npx serve -s . -l 8080",
    "build": "echo 'No build step needed'"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
